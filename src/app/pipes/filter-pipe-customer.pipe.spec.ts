import { FilterPipeCustomerPipe } from './filter-pipe-customer.pipe';

describe('FilterPipeCustomerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeCustomerPipe();
    expect(pipe).toBeTruthy();
  });
});
