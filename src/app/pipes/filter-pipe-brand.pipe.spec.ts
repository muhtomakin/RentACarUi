import { FilterPipeBrandPipe } from './filter-pipe-brand.pipe';

describe('FilterPipeBrandPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeBrandPipe();
    expect(pipe).toBeTruthy();
  });
});
