import { FilterPipeRentalPipe } from './filter-pipe-rental.pipe';

describe('FilterPipeRentalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeRentalPipe();
    expect(pipe).toBeTruthy();
  });
});
