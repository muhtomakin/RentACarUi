import { FilterPipeCarPipe } from './filter-pipe-car.pipe';

describe('FilterPipeCarPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeCarPipe();
    expect(pipe).toBeTruthy();
  });
});
