import { FilterPipeColorPipe } from './filter-pipe-color.pipe';

describe('FilterPipeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
